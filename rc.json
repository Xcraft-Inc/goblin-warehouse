{
  "upsert": {
    "parallel": true,
    "desc": "update or insert a new state branch",
    "options": {
      "params": {
        "required": "branch",
        "optional": "data"
      }
    }
  },
  "subscribe": {
    "parallel": true,
    "desc": "create a subscription feed",
    "options": {
      "params": {
        "required": "feed",
        "optional": "branches..."
      }
    }
  },
  "unsubscribe": {
    "parallel": true,
    "desc": "unsubscribe from a feed",
    "options": {
      "params": {
        "required": "feed"
      }
    }
  }
}
